{
  "name": "growi-mcp-server",
  "version": "1.7.1",
  "description": "MCP server that connects AI models to GROWI wiki content. Enables searching, reading, and creating pages in your GROWI knowledge base.",
  "settings": [
    {
      "name": "Default App Name",
      "description": "The default GROWI app name to use",
      "envVar": "GROWI_DEFAULT_APP_NAME"
    },
    {
      "name": "App Name",
      "description": "Name identifier for the GROWI app",
      "envVar": "GROWI_APP_NAME_1"
    },
    {
      "name": "API Token",
      "description": "API token for authenticating with GROWI",
      "envVar": "GROWI_API_TOKEN_1",
      "sensitive": true
    },
    {
      "name": "Base URL",
      "description": "Base URL of the GROWI instance (e.g. https://your-growi.example.com)",
      "envVar": "GROWI_BASE_URL_1"
    }
  ],
  "// mcpServers.growi.cwd": "The parent of the extension directory is used on purpose: the extension directory itself holds a package.json named @growi/mcp-server, so npx resolves that local package instead of the published one and fails with mcp-server: not found. Leaving cwd out is worse, because npx then runs inside whatever project the user started gemini in: that project's .npmrc decides which registry is used, and its .env files leak GROWI settings into this server.",
  "mcpServers": {
    "growi": {
      "command": "npx",
      "args": ["-y", "@growi/mcp-server@1.7.1"],
      "cwd": "${extensionPath}${/}.."
    }
  }
}
